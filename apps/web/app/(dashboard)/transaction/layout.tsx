import React from "react";
import type { PropsWithChildren } from "react";

const TransactionLayout: React.FC<PropsWithChildren> = ({ children }) => {
  return children;
};

export default TransactionLayout;
